version: "2.1"

services:
  app:
    build: app
    labels:
      io.balena.features.balena-api: 1
    volumes:
      - keys:/keys
      - cache:/cache
      - backups:/backups
      - config:/config
    privileged: true

volumes:
  keys:
  cache:
  backups:
  config:
